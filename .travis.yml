sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t 1919sai/react-test -f ./client/Dockerfile.dev ./client

script:
  - docker run -e CI=true 1919sai/react-test npm test -- --coverage

after_success:
  - docker build -t 1919sai/multi-client ./client
  - docker build -t 1919sai/multi-nginx ./nginx
  - docker build -t 1919sai/multi-server ./server
  - docker build -t 1919sai/multi-worker ./worker
  # Log in to the docker CLI
  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
  # Take those images and push them to docker hub
  - docker push 1919sai/multi-client
  - docker push 1919sai/multi-nginx
  - docker push 1919sai/multi-server
  - docker push 1919sai/multi-worker
